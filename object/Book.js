class Book {
    constructor(img_url, title, wirter, summary, genre) {
        this.img_url = img_url
        this.title = title
        this.wirter = wirter
        this.summary = summary
        this.genre = genre
    }
}